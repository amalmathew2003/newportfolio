import 'package:flutter/material.dart';

/// Stub — should never be called at runtime.
/// Replaced by download_web.dart or download_mobile.dart via conditional import.
Future<void> downloadCVPlatform(BuildContext context) async {
  throw UnsupportedError('Cannot download CV on this platform.');
}
